#include "main.h"

/**
 * swap_int - it swaps the values of 2 intergers
 * @a: input interger pointer
 * @b: input integer pointer
 */
void swap_int(int *a, int *b)
{
	int n;

	n = *a;
	*a = *b;
	*b = n;
)

