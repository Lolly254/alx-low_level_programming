#include "main'h"

/**
 * _abs - gives the absolute value
 * of an integer.
 *
 * @k: input number as an integer.
 *
 * Return: absolute value
 *
 */
int _abs(int k)
{
int k;
if (k >= 0)
{
return (k);
}
else
{
return (k * -1);
}
}

